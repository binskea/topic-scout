"""Milestone 11 (`PLAN.md`): the eval harness and its scenarios.

Not part of the shipped skill payload's runtime path (`curiosity-radar/`'s
`src/curiosity_radar` is), but lives alongside it in the same repo — see
`evals/README.md` for the exact invocation.
"""
